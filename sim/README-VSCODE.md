Do not commit editor-specific workspace files.

This project ignores the `.vscode/` directory. Avoid committing `c_cpp_properties.json`, `settings.json`, or other editor-generated files.

If your editor auto-generates these files, configure it to use a global user-level settings location or disable automatic workspace file generation.
